#include "behavior.h"
#include <stdio.h>

void cheer_for(const char* fan, const char* star) {
    printf("%s's fans cheer for %s!\n", fan, star);
}

void boo_for(const char* fan, const char* star) {
    printf("%s's fans boo for %s!\n", fan, star);
}
